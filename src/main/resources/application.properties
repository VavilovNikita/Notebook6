# === Spring Application ===
spring.application.name=CodeSage

# === DataSource ===
spring.datasource.url=jdbc:postgresql://localhost:5433/notebook
spring.datasource.username=user
spring.datasource.password=1234

# === JPA & Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# === Liquibase ===
spring.liquibase.change-log=classpath:db/changelog/changelog.xml

# === Spring MVC ===
spring.mvc.hiddenmethod.filter.enabled=true

# === Security (default user) ===
spring.security.user.name=user
spring.security.user.password=root

# === Thymeleaf ===
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false

# === Server ===
server.error.whitelabel.enabled=false

# === DeepSeek API ===
deepseek.model=deepseek-chat
deepseek.stream=false

deepseek.system.role=system
deepseek.system.content=??????????, ????????? JSON-?????? ? ???? "recommendations". ?????? ???????????? ?????? ?????????: issue, description, suggestion, example, note. ?? ????????? markdown, ?????? JSON. ??????: {"recommendations":[{"issue":"...", "description":"...", "suggestion":"...", "example":"...", "note":"..."}]}

deepseek.system.example=??? ?????? ????, ??? ???????? ?????????? ?????: {"recommendations":[{"issue":"...", "description":"...", "suggestion":"...", "example":"...", "note":"..."}]} ?? ???????

deepseek.system.You-analysis-assistant=?? ????????? ?? ???????

deepseek.user.role=user

server.servlet.session.timeout=3153600000

# === minio ===
minio.url=http://localhost:9000
minio.access-key=minioadmin
minio.secret-key=minioadmin
minio.bucket=subtitles